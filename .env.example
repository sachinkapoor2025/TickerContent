JWT_SECRET=dev-only-change-me
PORT=3001
WEB_ORIGIN=http://localhost:5173
# Optional Postgres. When unset, the API uses embedded PGlite (no Docker).
# DATABASE_URL=postgres://ticker:ticker@localhost:5432/ticker_cms
